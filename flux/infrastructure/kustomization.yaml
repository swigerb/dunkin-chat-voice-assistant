apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespaces.yaml
  - sources/helm-repos.yaml
  - cert-manager/helmrelease.yaml
  - trust-manager/helmrelease.yaml
  # - foundry-operator/helmrelease.yaml  # Disabled: chart has YAML error (duplicate label key)
  - nvidia-device-plugin/daemonset.yaml
