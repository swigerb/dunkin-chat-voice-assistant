apiVersion: v1
kind: Service
metadata:
  name: whisper-stt
  namespace: dunkin-voice
  labels:
    app.kubernetes.io/name: whisper-stt
    app.kubernetes.io/component: speech-to-text
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: whisper-stt
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
